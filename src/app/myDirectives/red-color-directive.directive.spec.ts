import { RedColorDirectiveDirective } from './red-color-directive.directive';

describe('RedColorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RedColorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
